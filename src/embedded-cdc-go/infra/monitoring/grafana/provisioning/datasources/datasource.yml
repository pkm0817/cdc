apiVersion: 1

datasources:
  - name: Prometheus
    uid: emb-cdc-go-prom
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 5s
