package dev.cdccustom.domain;

/**
 * 중복 접기의 기준. 한 배치 안에서 같은 (표, 행)에 여러 번 변경이 있었다면
 * 마지막 것 하나만 남긴다.
 *
 * <p>이 한 줄이 이 방식의 속도 대부분을 만든다. 부하 시험에서 같은 행을 반복
 * 갱신하면 CDC 는 갱신 횟수만큼 타깃에 쓰지만, 여기서는 배치당 한 번만 쓴다.
 * 반대로 서로 다른 행만 한 번씩 바뀌는 부하에서는 접힐 것이 없어 이득이 없다 —
 * 이 방식의 이득이 부하 모양을 탄다는 뜻이다.
 */
public record ChangeKey(String table, long rowId) {
}
